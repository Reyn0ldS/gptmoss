---
name: secure-python
description: Implement and review Python safely with tests and input validation.
allowed_capabilities: [filesystem, shell]
---
Write complete Python code. Validate external inputs, avoid exposing secrets, add focused tests, and run tests before reporting completion. Use filesystem actions for edits; shell commands require approval.
