# GPT-Moss Environment Configuration Template
OPENAI_API_KEY=mock-key
OPENAI_BASE_URL=https://gpu01.quartz.moss/general/v1
OPENAI_MODEL_NAME=Qwen/Qwen3.6-35B
# Applied only when the equivalent key is absent from workspace/config.json
# (first run without a copied template). A saved GUI/config.json value wins.
# SSL_VERIFY=False
# SSL_CERT_PATH=
# Listen address used by main.py and the supervisor when --host/--port are omitted.
MOSS_HOST=127.0.0.1
MOSS_PORT=8000
